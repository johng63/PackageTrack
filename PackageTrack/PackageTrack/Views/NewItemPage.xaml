<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="PackageTrack.Views.NewItemPage"
		     Title="New Item">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Scan" Order="Secondary" Clicked="Scan_Clicked" />
        <ToolbarItem Text="Save" Order="Secondary" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="BarCode" FontSize="Medium" />
            <Entry Text="{Binding Item.BarCode}" FontSize="Small" />
            <Label Text="Checked In User" FontSize="Medium" />
            <Entry Text="{Binding Item.CheckInUser}" FontSize="Small" />
            <Label Text="Description" FontSize="Medium" />
            <Editor Text="{Binding Item.Description}" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
